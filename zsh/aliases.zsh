# ps
alias psg="ps aux | grep "
alias psr='ps aux | grep ruby'

# human friendly df
alias df='df -h'

# use the MacVim version of Vim
alias vim="mvim -v"

# err'yday i'm zippin
alias gz='tar -zcvf'

# kill
alias ka9='killall -9'
alias k9='kill -9'

# common functions
alias ps='ps aux'
alias cl='clear'
alias less='less -r'

# bundler, you crazy
alias bx='bundle exec'
